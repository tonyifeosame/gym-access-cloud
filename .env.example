# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=gym_admin
DB_PASSWORD=your_secure_password
DB_NAME=gym_access

# Server Configuration
SERVER_PORT=8080
GIN_MODE=release

# JWT Secret (for future token-based auth if needed)
JWT_SECRET=your_jwt_secret_key_here
